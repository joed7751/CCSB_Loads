YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
library(loadflex)
library(rloadest)
library(dataRetrieval)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISuv(siteNumber, parameterCd,
"2010-10-01", "2019-10-01")
Road102Q <- renameNWISColumns(dischargeUnit)
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(Road102Q) = c("Dates", "Flow")        #changing columns name
Road102Q1 <- subset(Road102Q, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(Road102Q1, file.name = "Road102Q_2010_2019.cvs")
Road102Q
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISuv(siteNumber, parameterCd,
"2010-10-01", "2019-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(YoloQ) = c("Dates", "Flow")        #changing columns name
YoloQ1 <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ1, file.name = "YoloQ_2010_2019.cvs")
YoloQ
library(loadflex)
library(rloadest)
library(dataRetrieval)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISuv(siteNumber, parameterCd,
"2010-09-01", "2019-10-01")
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(Road102Q) = c("Dates", "Flow")        #changing columns name
Road102Q1 <- subset(Road102Q, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(Road102Q1, file.name = "Road102Q_2010_2019.cvs")
Road102Q
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISuv(siteNumber, parameterCd,
"2010-09-01", "2019-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(YoloQ) = c("Dates", "Flow")        #changing columns name
YoloQ1 <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ1, file.name = "YoloQ_2010_2019.cvs")
YoloQ
dischargeUnit <- readNWISuv(siteNumber, parameterCd,
"2010-10-01", "2019-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(YoloQ) = c("Dates", "Flow")        #changing columns name
YoloQ1 <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ1, file.name = "YoloQ_2010_2019.cvs")
YoloQ
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISuv(siteNumber, parameterCd,
"2009-10-01", "2010-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(YoloQ) = c("Dates", "Flow")        #changing columns name
YoloQ1 <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ1, file.name = "YoloQ_2009_2010only.cvs")
library(loadflex)
library(rloadest)
library(dataRetrieval)
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISuv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(YoloQ) = c("Dates", "Flow")        #changing columns name
YoloQ1 <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ1, file.name = "YoloQ_2009_2017_0521.cvs")
YoloQ
library(loadflex)
library(rloadest)
library(dataRetrieval)
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISuv(siteNumber, parameterCd,
"2010-01-19", "2010-01-22")
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(Road102Q) = c("Dates", "Flow")        #changing columns name
Road102Q1 <- subset(Road102Q, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(Road102Q1, file.name = "Road102Q_2010_2019.cvs")
Road102Q
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISuv(siteNumber, parameterCd,
"2010-01-19", "2010-01-22")
View(Road102Q)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISuv(siteNumber, parameterCd,
"2010-01-19", "2010-01-22")
library(loadflex)
library(rloadest)
library(dataRetrieval)
?readNWISuv
?readNWISdv
?parameterCd
?readNWISuv
?readNWISdv
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01") #pulling  out the 15 min Q data from NWIS
YoloQDv <- renameNWISColumns(dischargeUnit)
YoloQDv <- YoloQDv[c("dateTime","Flow_Inst")] #pulling out only the columns we need
View(YoloQDv)
YoloQDv <- YoloQDv[c("Date","Flow")] #pulling out only the columns we need
colnames(YoloQDv) = c("Dates", "Flow")        #changing columns name
YoloQDv <- subset(YoloQDv, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQDv, file.name = "YoloQ_2009_2017DV.cvs")
YoloQ
YoloQDV
exportCSV(YoloQDv, file.name = "YoloQ_2009_2017DV.cvs")
YoloQDV
YoloQDv <- subset(YoloQDv, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
YoloQDv
dischargeUnit <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01") #pulling  out the 15 min Q data from NWIS
YoloQDv <- renameNWISColumns(dischargeUnit)
colnames(YoloQDv) = c("Dates", "Flow")        #changing columns name
YoloQDv <- subset(YoloQDv, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
View(YoloQDv)
dischargeUnit <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01") #pulling  out the 15 min Q data from NWIS
View(dischargeUnit)
YoloQDv <- renameNWISColumns(dischargeUnit)
YoloQDv <- YoloQDv[c("Date","Flow")] #pulling out only the columns we need
colnames(YoloQDv) = c("Dates", "Flow")        #changing columns name
YoloQDv <- subset(YoloQDv, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQDv, file.name = "YoloQ_2009_2017DV.cvs")
YoloQDv
library(loadflex)
library(rloadest)
library(dataRetrieval)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01")
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452600" #Road 102#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01")
View(Road102Q)
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("dateTime","Flow_Inst")] #pulling out only the columns we need
View(Road102Q)
Road102Q <- Road102Q[c("Site_no","Date","Flow")] #pulling out only the columns we need
View(Road102Q)
View(Road102Q)
Road102Q <- Road102Q[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(Road102Q) = c("Dates", "Flow")        #changing columns name
View(Road102Q)
colnames(Road102Q) = c("Site","Dates", "Flow")        #changing columns name
View(Road102Q)
Road102Q1 <- subset(Road102Q, Dates < as.POSIXct("2017-10-01 00:00:00", tz="EST5EDT"))
Road102Q <- subset(Road102Q, Dates < as.POSIXct("2017-10-01 00:00:00", tz="EST5EDT"))
exportCSV(Road102Q, file.name = "Road102QDaily_2010_2019.cvs")
Road102Q
###Flow Date
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2017-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(YoloQ) = c("Site","Dates", "Flow")        #changing columns name
YoloQ <- subset(YoloQ, Dates < as.POSIXct("2017-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ, file.name = "YoloQDaily_2009_2017.cvs")
YoloQ
library(loadflex)
library(rloadest)
library(dataRetrieval)
siteNumber <-"11452500" #this is to set up the site
parameterCd <- "00060" # Discharge
dischargeUnit <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-10-01") #pulling  out the 15 min Q data from NWIS
YoloQ <- renameNWISColumns(dischargeUnit)
YoloQ <- YoloQ[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(YoloQ) = c("Site","Dates", "Flow")        #changing columns name
YoloQ <- subset(YoloQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(YoloQ, file.name = "YoloQDaily_2009_2019.cvs")
YoloQ
library(loadflex)
library(rloadest)
library(dataRetrieval)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-10-01")
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-10-01")
View(Road102Q)
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-03-30")
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-03-30")
View(Road102Q)
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("site_no","Date","Flow")] #pulling out only the columns we need
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
Road102Q <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
Road102Q <- renameNWISColumns(Road102Q)
Road102Q <- Road102Q[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(Road102Q) = c("Site","Dates", "Flow")        #changing columns name
Road102Q <- subset(Road102Q, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(Road102Q, file.name = "SouthQDaily_2010_2019.cvs")
Road102Q
View(Road102Q)
library(loadflex)
library(rloadest)
library(dataRetrieval)
###################################################
#Pulling Flow Data
###################################################
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
SouthQ <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
SouthQ <- renameNWISColumns(SouthQ)
SouthQ <- SouthQ[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(SouthQ) = c("Site","Dates", "Flow")        #changing columns name
SouthQ <- subset(SouthQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(SouthQ, file.name = "SouthQDaily_2010_2019.cvs")
SouthQ
South_UQ <- readNWISuv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
South_UQ <- renameNWISColumns(SouthQ)
South_UQ <- SouthQ[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(South_UQ) = c("Site","Dates", "Flow")        #changing columns name
SouthQ <- subset(South_UQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(South_UQ, file.name = "SouthQ_Unit_2010_2019.cvs")
South_UQ
View(South_UQ)
South_UQ <- readNWISuv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
South_UQ <- renameNWISColumns(South_UQ)
South_UQ <- SouthQ[c("site_no","Date","Flow")] #pulling out only the columns we need
South_UQ <- South_UQ[c("site_no","Date","Flow")] #pulling out only the columns we need
View(South_UQ)
South_UQ <- South_UQ[c("site_no","dateTime","Flow_Inst")] #pulling out only the columns we need
View(South_UQ)
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
SouthQ <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
SouthQ <- renameNWISColumns(SouthQ)
SouthQ <- SouthQ[c("site_no","Date","Flow")] #pulling out only the columns we need
colnames(SouthQ) = c("Dates", "Flow")        #changing columns name
SouthQ <- subset(SouthQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(SouthQ, file.name = "SouthQDaily_2010_2019.cvs")
SouthQ
View(SouthQ)
colnames(SouthQ) = c("STID","Dates", "Flow")        #changing columns name
View(SouthQ)
colnames(SouthQ) = c("STEID","Dates", "Flow")        #changing columns name
SouthQ <- subset(SouthQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(SouthQ, file.name = "SouthQDaily_2010_2019.cvs")
SouthQ
South_UQ <- readNWISuv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
South_UQ <- renameNWISColumns(South_UQ)
South_UQ <- South_UQ[c("dateTime","Flow_Inst")] #pulling out only the columns we need
colnames(South_UQ) = c("Dates", "Flow")        #changing columns name
SouthQ <- subset(South_UQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(South_UQ, file.name = "SouthQ_Unit_2010_2019.cvs")
South_UQ
View(South_UQ)
SouthQ <- SouthQ[c("Date","Flow")] #pulling out only the columns we need
siteNumber <-"11452800" #South Abutment#
parameterCd <- "00060" # Discharge
SouthQ <- readNWISdv(siteNumber, parameterCd,
"2009-10-01", "2019-09-30")
SouthQ <- renameNWISColumns(SouthQ)
SouthQ <- SouthQ[c("Date","Flow")] #pulling out only the columns we need
colnames(SouthQ) = c("Dates", "Flow")        #changing columns name
SouthQ <- subset(SouthQ, Dates < as.POSIXct("2019-10-01 00:00:00", tz="EST5EDT"))
exportCSV(SouthQ, file.name = "SouthQDaily_2010_2019.cvs")
SouthQ
View(SouthQ)

siteNumbers<-"11452500"  
yolommt<-readNWISmeas(siteNumbers,expanded=TRUE,tz="UTC")
yolommt<-subset(yolommt,select=c(measurement_dateTime,chan_velocity,discharge_va))
vel_yolo.move.1<-move.1(chan_velocity~log(discharge_va),yolommt,na.action=na.exclude,distribution="normal")
print(vel_yolo.move.1)
setSweave("regplot01",5,5)
plot(vel_yolo.move.1,which=1)
plot(vel_yolo.move.1,which=2)
plot(vel_yolo.move.1,which=3)
plot(vel_yolo.move.1,which=4)
plot(vel_yolo.move.1,which=5)
plot(vel_yolo.move.1,which=6)
plot(vel_yolo.move.1,which="PERIM")
yoloIVQ_vel<-subset(yoloIVQ_f, select=c(dateTime,Q))
names(yoloIVQ_vel)[2]<-"discharge_va"
predictVEL<-predict(vel_yolo.move.1,yoloIVQ_vel,type="response")
predictVEL<-as.data.frame(vel_yolo.move.1)
exportCSV(predict102.move.1,file.name="pred102mv1.csv")
exportCSV()
